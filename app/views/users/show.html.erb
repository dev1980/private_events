<h1><%= @user.name %> </h1>
<h2>Events Created By <%= @user.name %>:</h2>
<%= render partial: 'shared/events', locals: { events: @user.created_events } %>
<h2>Events Attended By User </h2>
<%= render partial: 'shared/events', locals: { events: @user.attended_events } %>



